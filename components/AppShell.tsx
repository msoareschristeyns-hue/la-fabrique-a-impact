'use client';
import Link from 'next/link';import{useEffect,useState,ReactNode}from'react';import{usePathname,useRouter}from'next/navigation';import{LayoutDashboard,ClipboardCheck,Target,ListChecks,FolderCheck,Activity,FileText,Building2,Settings,LogOut,Library,Files,GraduationCap,ShieldCheck}from'lucide-react';import{supabase}from'../lib/supabase';import CopilotFloating from './CopilotFloating';const nav=[['/dashboard/','Vue d’ensemble',LayoutDashboard],['/diagnostic/','Diagnostic RSE',ClipboardCheck],['/priorities/','Mes 3 priorités',Target],['/actions/','Plan d’action',ListChecks],['/proofs/','Mes preuves',FolderCheck],['/library/','Bibliothèque RSE',Library],['/workspace/','Espace entreprise',Files],['/learning/','Sensibilisation & formation',GraduationCap],['/progress/','Ma progression',Activity],['/report/','Synthèse RSE',FileText]]as const;
export default function AppShell({children}:{children:ReactNode}){const pathname=usePathname();const router=useRouter();const[company,setCompany]=useState('');const[ready,setReady]=useState(false);const[platformAdmin,setPlatformAdmin]=useState(false);useEffect(()=>{let mounted=true;(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/connexion/');return}const[{data},{data:a}]=await Promise.all([supabase.from('company_members').select('company_id,companies(name)').eq('user_id',user.id).limit(1).maybeSingle(),supabase.from('platform_admins').select('user_id').eq('user_id',user.id).maybeSingle()]);const row:any=data;if(mounted){setCompany(row?.companies?.name||'Mon entreprise');setPlatformAdmin(!!a);setReady(true)}})();const onCompany=(e:Event)=>{const detail=(e as CustomEvent<{name?:string}>).detail;if(detail?.name)setCompany(detail.name)};window.addEventListener('company-updated',onCompany);return()=>{mounted=false;window.removeEventListener('company-updated',onCompany)}},[router]);async function logout(){await supabase.auth.signOut();router.replace('/connexion/')}if(!ready)return <main className="formPage"><div className="formWrap"><p>Chargement de votre espace entreprise…</p></div></main>;return <main className="dash"><aside><Link href="/dashboard/" className="brand"><img className="brandLogo" src="/logo-la-fabrique-impact.svg" alt="La Fabrique à Impact"/></Link><div className="companyBadge"><Building2/><div><small>ENTREPRISE</small><b>{company}</b></div></div><nav>{nav.map(([href,label,Icon])=><Link key={href} className={pathname===href||pathname===href.slice(0,-1)?'active':''} href={href}><Icon/>{label}</Link>)}</nav><div className="sideBottom">{platformAdmin&&<Link className={(pathname==='/admin/'||pathname==='/admin')?'settings activeSetting':'settings'} href="/admin/"><ShieldCheck/>Administration</Link>}<Link className={(pathname==='/settings/'||pathname==='/settings')?'settings activeSetting':'settings'} href="/settings/"><Settings/>Paramètres</Link><button className="settings shellLogout" onClick={logout}><LogOut/>Déconnexion</button></div></aside><section className="dashMain">{children}</section><CopilotFloating/></main>}
