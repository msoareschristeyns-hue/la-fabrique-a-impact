'use client';
import{useEffect,useState}from'react';import{Building2,ImagePlus,Palette,Save,ShieldCheck,UserRound}from'lucide-react';import{supabase}from'../../lib/supabase';
const sectors=['Industrie','Commerce','Services','BTP','Transport & logistique','Autre'];const sizes=['1–9 personnes','10–49 personnes','50–249 personnes','250+ personnes'];
export default function SettingsPage(){const[companyId,setCompanyId]=useState('');const[name,setName]=useState('');const[sector,setSector]=useState('');const[size,setSize]=useState('');const[role,setRole]=useState('');const[email,setEmail]=useState('');const[status,setStatus]=useState('');const[saving,setSaving]=useState(false);const[logoUrl,setLogoUrl]=useState('');const[primary,setPrimary]=useState('#153b46');const[secondary,setSecondary]=useState('#0797d5');const[logoFile,setLogoFile]=useState<File|null>(null);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user)return;setEmail(user.email||'');const{data:m}=await supabase.from('company_members').select('company_id,role,companies(name,sector,size_band,logo_url,brand_primary,brand_secondary)').eq('user_id',user.id).limit(1).single();const row:any=m;if(row){setCompanyId(row.company_id);setRole(row.role||'member');setName(row.companies?.name||'');setSector(row.companies?.sector||'');const raw=row.companies?.size_band||'';setSize(raw&&raw.includes('personnes')?raw:raw?`${raw} personnes`:'');setLogoUrl(row.companies?.logo_url||'');setPrimary(row.companies?.brand_primary||'#153b46');setSecondary(row.companies?.brand_secondary||'#0797d5')}})()},[]);async function save(){if(!companyId||!name.trim())return;setSaving(true);setStatus('');let nextLogo=logoUrl;try{if(logoFile){const ext=logoFile.name.split('.').pop()?.toLowerCase()||'png';const path=`${companyId}/logo.${ext}`;const{error:uploadError}=await supabase.storage.from('company-branding').upload(path,logoFile,{upsert:true,contentType:logoFile.type||undefined});if(uploadError)throw uploadError;nextLogo=supabase.storage.from('company-branding').getPublicUrl(path).data.publicUrl}const{error}=await supabase.from('companies').update({name:name.trim(),sector:sector||null,size_band:size||null,logo_url:nextLogo||null,brand_primary:primary,brand_secondary:secondary,updated_at:new Date().toISOString()}).eq('id',companyId);if(error)throw error;setLogoUrl(nextLogo);setLogoFile(null);setStatus('Informations et identité visuelle enregistrées.');window.dispatchEvent(new CustomEvent('company-updated',{detail:{name:name.trim()}}))}catch{setStatus('Impossible d’enregistrer les modifications.')}setSaving(false)}const canEdit=role==='owner'||role==='admin';return <div className="modulePage"><div className="moduleHead"><span className="kicker">PARAMÈTRES</span><h1>Votre entreprise, votre identité visuelle et votre compte.</h1><p>Ces informations servent à contextualiser votre parcours RSE et à personnaliser les documents générés dans votre espace entreprise.</p></div><div className="settingsGrid"><section className="settingsCard"><div className="settingsTitle"><Building2/><div><small>ENTREPRISE</small><h2>Informations générales</h2></div></div><label>Nom de l’entreprise<input value={name} disabled={!canEdit} onChange={e=>setName(e.target.value)}/></label><label>Secteur d’activité<select value={sector} disabled={!canEdit} onChange={e=>setSector(e.target.value)}><option value="">Non renseigné</option>{sectors.map(x=><option key={x}>{x}</option>)}</select></label><label>Effectif<select value={size} disabled={!canEdit} onChange={e=>setSize(e.target.value)}><option value="">Non renseigné</option>{sizes.map(x=><option key={x}>{x}</option>)}</select></label><div className="settingsTitle"><Palette/><div><small>IDENTITÉ VISUELLE</small><h2>Logo & charte graphique</h2></div></div>{logoUrl&&<div style={{padding:16,border:'1px solid #dce7e5',borderRadius:12,background:'#fff'}}><img src={logoUrl} alt={`Logo ${name}`} style={{maxWidth:190,maxHeight:85,objectFit:'contain'}}/></div>}<label><span>Logo de l’entreprise</span><input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml" disabled={!canEdit} onChange={e=>setLogoFile(e.target.files?.[0]||null)}/><small>PNG, JPG, WEBP ou SVG · 2 Mo maximum.</small></label><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:14}}><label>Couleur principale<div style={{display:'flex',gap:8}}><input type="color" value={primary} disabled={!canEdit} onChange={e=>setPrimary(e.target.value)} style={{width:52,padding:4}}/><input value={primary} disabled={!canEdit} onChange={e=>setPrimary(e.target.value)}/></div></label><label>Couleur secondaire<div style={{display:'flex',gap:8}}><input type="color" value={secondary} disabled={!canEdit} onChange={e=>setSecondary(e.target.value)} style={{width:52,padding:4}}/><input value={secondary} disabled={!canEdit} onChange={e=>setSecondary(e.target.value)}/></div></label></div><div style={{display:'flex',alignItems:'center',gap:12,padding:14,border:'1px solid #dce7e5',borderRadius:12}}><ImagePlus/><div style={{display:'flex',gap:6,flex:1}}><span style={{height:16,flex:1,borderRadius:8,background:primary}}/><span style={{height:16,flex:1,borderRadius:8,background:secondary}}/></div><small>Aperçu de la charte</small></div>{canEdit?<button className="button" disabled={saving||!name.trim()} onClick={save}><Save/>{saving?'Enregistrement…':'Enregistrer'}</button>:<p className="settingsHint">Seuls les propriétaires et administrateurs peuvent modifier ces informations.</p>}{status&&<p className={status.startsWith('Impossible')?'auth-error':'auth-success'}>{status}</p>}</section><section className="settingsCard"><div className="settingsTitle"><UserRound/><div><small>COMPTE</small><h2>Votre accès</h2></div></div><div className="accountRow"><span>Adresse de connexion</span><b>{email||'—'}</b></div><div className="accountRow"><span>Rôle dans l’entreprise</span><b>{role||'—'}</b></div><div className="securityNote"><ShieldCheck/><div><b>Espace entreprise sécurisé</b><p>Les données sont isolées par entreprise grâce aux règles d’accès de la base Supabase.</p></div></div></section></div></div>}
